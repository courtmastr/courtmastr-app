# Release v2.18.0+deploy.2

- Status: deployed
- Package version: `2.18.0`
- Release date: 2026-04-19 01:51 CDT
- Release commit: `fc2ebbc`
- Release branch: `master`

## Summary

This release record captures a second production deploy on package version `2.18.0` after the original `v2.18.0` metadata PR merged later than the deploy itself. The build-metadata release ID keeps the later production state distinct without inventing a patch version that was never declared in `package.json`.

## Highlights

- Fix pool standings and public bracket routing (#90)
- Includes the earlier `v2.18.0` tournament scoring fixes that were already part of the same package version.
- Updates the deploy record to the later production state while preserving the original `v2.18.0` release note.

## Verification

- Production deploy recorded with:
  - `npm run release:deploy`
  - `npm run deploy`
  - `npm run deploy:log`
- Evidence recorded in [LAST_DEPLOY.md](/Users/ramc/Documents/Code/courtmaster-v2/docs/deployment/LAST_DEPLOY.md).
- Build/deploy log artifact:
  - `docs/debug-kb/_artifacts/2026-04-19-06-48-38.npm-run-build-firebase-deploy-project-production.log`

## Deployment

- Firebase project: `courtmaster-v2`
- Hosting URL: `https://courtmaster-v2.web.app`
- Deploy result:
  - Release automation completed all guardrails before Firebase deploy
  - Includes Firebase/backend, tournament operations, and public experience changes from `fc2ebbc`

## Risks / Follow-Ups

- This deploy reused package version `2.18.0`, which should remain an exception rather than a normal release pattern.
- Future production deploys should continue to advance the semantic version before rollout.
