---
id: 6b531036
title: "eslint not found when running lint:log"
signature: "eslint: command not found"
area: lint
status: ❌ unresolved
tags: [eslint, lint, missing-bin]
artifact: 2026-02-18-21-35-55.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: eslint not found when running lint:log

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Node version: `v22.22.0`
- Firebase tools version: `command not found (firebase --version)`
- Recent changes: Added real-time listeners in `src/features/brackets/components/BracketsManagerViewer.vue`.

## Error Signature
```
/bin/sh: eslint: command not found
```

## Attempts

### Attempt 1
**Date**: 2026-02-01
**Change**: No fix attempted; documented the failure for follow-up.

**Result**:
❌ Same error

### Attempt 2
**Date**: 2026-02-03
**Change**: Re-ran `npm run lint:log` after bracket viewer updates.

**Result**:
❌ Same error

### Attempt 3
**Date**: 2026-02-04
**Change**: Ran `npm install`, then re-ran `npm run lint:log`.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 4
**Date**: 2026-02-04
**Change**: Re-ran `npm run lint:log` after additional dashboard/test changes.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 5
**Date**: 2026-02-11
**Change**: Ran the documented final fix sequence: `npm install` followed by `npm run lint:log`.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 6
**Date**: 2026-02-15
**Change**: Re-ran KB final fix exactly: `npm install` then `npm run lint:log`.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 7
**Date**: 2026-02-16
**Change**: Re-ran KB final fix in TOURNEY-103 worktree: `npm install` then `npm run lint:log`.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 8
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` during tournament creation + registration import fixes. Reviewed KB final fix, but did not install new dependency because task contract forbids adding dependencies unless explicitly requested.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 9
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` during pool-to-elimination starter implementation and confirmed the same missing binary path issue before lint execution begins.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 10
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` during pool level-splitting implementation (Create Levels flow and level bracket generation).

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 11
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after normalization fixes (shared level defaults, ID hydration hardening, pool mapping payload consistency). Re-checked KB final fix; skipped dependency install because this task disallows adding dependencies without explicit approval.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 12
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after UI clarity fix for Create Levels (provisional rank display + disabled manual overrides before pool completion).

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 13
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after Firestore rules update for level generation paths (`levels`, `level_assignments`, `level_generation`).

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 14
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after replacing `scripts/seed-simple.ts` to generate the new 3-category tournament and auto-complete Men's Doubles pool matches.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 15
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after updating seed-name generation/team naming. Re-checked KB final fix; skipped `npm install --save-dev eslint` because task rules disallow adding dependencies unless explicitly requested.

**Result**:
❌ Same error (`eslint` still not found)

### Attempt 16
**Date**: 2026-02-18
**Change**: Re-ran `npm run lint:log` after fixing team-name labels in `scripts/seed-production.ts`. Re-checked KB final fix; skipped `npm install --save-dev eslint` because task rules disallow adding dependencies unless explicitly requested.

**Result**:
❌ Same error (`eslint` still not found)

## Root Cause
Project scripts call `eslint` directly, but `eslint` is not present in `dependencies` or `devDependencies`, so no local binary exists in `node_modules/.bin`.

## Fix (final)
Not resolved in this ticket. Next step to try:
```bash
npm install --save-dev eslint
npm run lint:log
```

## Verification
- [ ] Lint passes: `npm run lint:log` exits 0

## Related Issues
- None

## Notes
- The failure occurred before any linting started, indicating a missing local dependency.
