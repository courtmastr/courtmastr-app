---
id: 8c707ab7
title: "lint:log fails due existing workspace-wide lint violations"
signature: "eslint . --ext ... --fix --ignore-path .gitignore reports unrelated project lint errors"
area: lint
status: ❌ unresolved
tags: [lint, baseline, workspace]
artifact: 2026-02-18-05-39-23.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails due existing workspace-wide lint violations

## Context
- Command: `npm run lint:log -- src/features/scoring/views/MatchListView.vue`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Switched Score Matches to shared `ManualScoreDialog` manual-entry flow

## Error Signature
```text
eslint . --ext ... --fix --ignore-path .gitignore <file args>
reports 13 errors across unrelated files (e2e/, scripts/, start-dev.mjs, TournamentDashboardView, etc.)
```

## Attempts

### Attempt 1
**Date**: 2026-02-18  
**Change**: Ran required `:log` lint command with a scoped file argument.  
**Result**: ❌ Fails on pre-existing repository lint baseline unrelated to current file.

## Root Cause
`lint:log` runs `eslint .` against the whole repository and the workspace has existing lint violations unrelated to this ticket.

## Fix (final)
Pending. Keep `npm run lint:log` as recorded evidence, and use direct scoped lint for touched files:
```bash
npx eslint src/features/scoring/views/MatchListView.vue
```

## Verification
- [ ] `npm run lint:log` passes across workspace
- [x] Scoped lint can validate touched files independently

## Related Issues
- `docs/debug-kb/77e0bc7c.md`
- `docs/debug-kb/626b5359.md`

## Notes
- This failure is baseline/tooling-related and not introduced by this change.
