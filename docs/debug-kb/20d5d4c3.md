---
id: 20d5d4c3
title: "build:log fails on PrimaryActionEvent union mismatch in CategoryRegistrationStats"
signature: "TS2322: 'publish-schedule' is not assignable to type PrimaryActionEvent"
area: build
status: ✅ fixed
tags: [build, typescript, baseline]
artifact: 2026-03-14-16-18-16.vue-tsc-b.log
---

# KB Entry: build:log fails on PrimaryActionEvent union mismatch in CategoryRegistrationStats

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Front Desk and scorer UI refresh in `src/features/checkin/` and `src/features/scoring/`.

## Error Signature
```
src/features/tournaments/components/CategoryRegistrationStats.vue(707,9): error TS2322:
Type '"publish-schedule"' is not assignable to type 'PrimaryActionEvent'.
```

## Attempts

### Attempt 1
**Date**: 2026-03-04  
**Change**: Ran required verification (`npm run build` and `npm run build:log`) after documentation-only updates.  
**Result**: 🟡 Same TypeScript error in pre-existing workspace code; no errors originated from the documentation change.

### Attempt 2
**Date**: 2026-03-04  
**Change**: Re-ran `npm run build` and `npm run build:log` after adding Debug KB entry/index updates for this fingerprint.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same TypeScript baseline error.

### Attempt 3
**Date**: 2026-03-11  
**Change**: Re-ran `npm run build:log` after the public registration player-only signup change.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 4
**Date**: 2026-03-11  
**Change**: Re-ran `npm run build:log` after adding volunteer access core helpers and callable wrappers.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 5
**Date**: 2026-03-11  
**Change**: Re-ran `npm run build:log` after adding the volunteer session store, access view, layout split, and volunteer route guards.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) after Task 3 fixes; once the temporary `VolunteerLayout` nullability issue was corrected, the build returned to the existing baseline error only.

### Attempt 6
**Date**: 2026-03-11  
**Change**: Re-ran `npm run build:log` after adding volunteer-aware check-in/scoring mutation routing and server callables.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 7
**Date**: 2026-03-11  
**Change**: Re-ran `npm run build:log` after fixing numeric primary-id queries in the Cloud Function Firestore adapter and adding `firestoreAdapter.test.ts`.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) once the temporary regression-test typing issue was corrected; the repository returned to the existing `CategoryRegistrationStats.vue` baseline only.

### Attempt 8
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after volunteer scorekeeper kiosk routing/mobile scoring updates and the bracket foreign-key adapter fix.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 9
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after resolving merge conflicts between sponsor-branding and local volunteer/public-player worktree changes.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 10
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after Playwright suite cleanup (feature-based projects/reporting and setup log-noise reduction in `e2e/auth.setup.ts`).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 11
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after Front Desk and scorer experience redesign updates.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 12
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after final responsive polish adjustments (replacing `v-sheet`/`v-spacer` usage in redesigned views to keep unit tests clean).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 13
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after Playwright suite cleanup and scoring-feature test stabilization (`e2e/auth.setup.ts`, `e2e/scorekeeper-flow.spec.ts`, `e2e/concurrent-five-scorers.spec.ts`).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 14
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after final Playwright test stabilization pass (`CP-066` pattern addition and scoring-concurrency assertion hardening).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 15
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after wiring volunteer access functions to managed secrets and setting production secret values.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 16
**Date**: 2026-03-12  
**Change**: Re-ran `npm run build:log` after adding production secret-check/deploy scripts (`secrets:volunteer:check:prod`, `deploy:functions:prod`).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 17
**Date**: 2026-03-13  
**Change**: Re-ran `npm run build:log` during Horizon 1 branding rollout verification (logo SVG updates, public shell/footer, Live badge, check-in branding, OBS watermark, homepage copy refresh, and printable QR card).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue`.

### Attempt 18
**Date**: 2026-03-13  
**Change**: Re-ran `npm run build` and `npm run build:log` after final Horizon 1 branding verification pass.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-13-15-40-34.vue-tsc-b.log`).

### Attempt 19
**Date**: 2026-03-13  
**Change**: Re-ran `npm run build` and `npm run build:log` after aligning printable QR auto-print with explicit `window.onload` behavior.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-13-15-42-01.vue-tsc-b.log`).

### Attempt 20
**Date**: 2026-03-13  
**Change**: Re-ran `npm run build` and `npm run build:log` after Horizon 2A brand foundation updates (public marketing routes/footer, featured homepage metrics, legal/about/pricing pages, white OBS mark, announcement card generator, and public shell JSON-LD).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-13-16-13-09.vue-tsc-b.log`).

### Attempt 21
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after completing Horizon 2A remaining operational-in-repo updates (owner/contact details, legal draft contact text, and featured metrics env default).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-02-01-23.vue-tsc-b.log`).

### Attempt 22
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after CourtMastr brand copy normalization (including AGENTS update), public footer layout correction inside `v-main`, and public footer wording refresh.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-02-13-32.vue-tsc-b.log`).

### Attempt 23
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after adding CP-068 documentation and AppLayout footer regression assertion updates.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-02-16-07.vue-tsc-b.log`).

### Attempt 24
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after full-repository CourtMastr text normalization and final footer/layout verification pass.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-02-17-56.vue-tsc-b.log`).

### Attempt 25
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after adding social-proof testimonial content, founder blurb integration, and expanded U.S.-oriented operational legal sections in public pages.  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-13-28-40.vue-tsc-b.log`).

### Attempt 26
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after Horizon 2A completion work (reviews moderation module, metadata composable, landing-page modular redesign, tournament public review CTA/list integration, and marketing-footer visibility tightening).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-15-18-43.vue-tsc-b.log`).

### Attempt 27
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after final review-test polish and announcement-card filename normalization (`courtmastr-announcement-card.png`).  
**Result**: 🟡 Same fingerprint (`20d5d4c3`) and same unrelated `PrimaryActionEvent` union mismatch in `CategoryRegistrationStats.vue` (artifact: `docs/debug-kb/_artifacts/2026-03-14-15-20-06.vue-tsc-b.log`).

### Attempt 28
**Date**: 2026-03-14  
**Change**: Updated `CategoryRegistrationStats.vue` primary action mapping for `level_publish` to emit the valid union event (`schedule-times`) and re-ran full verification (`npm run build`, `npm run build:log`).  
**Result**: ✅ Build and build log now pass with no fingerprint for this issue (artifact: `docs/debug-kb/_artifacts/2026-03-14-15-58-37.vue-tsc-b.log`).

### Attempt 29
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after Horizon 2 brand-polish pass (CourtMastr lockup text correction, public review prompt polish, and footer/accessibility refinements).  
**Result**: ✅ Build and build log remain passing with no recurrence of fingerprint `20d5d4c3` (artifact: `docs/debug-kb/_artifacts/2026-03-14-16-15-46.vue-tsc-b.log`).

### Attempt 30
**Date**: 2026-03-14  
**Change**: Re-ran `npm run build` and `npm run build:log` after final Debug KB/index synchronization updates.  
**Result**: ✅ Build and build log still pass with no recurrence of fingerprint `20d5d4c3` (artifact: `docs/debug-kb/_artifacts/2026-03-14-16-18-16.vue-tsc-b.log`).

## Root Cause
`CategoryRegistrationStats.vue` used a primary-action literal (`publish-schedule`) that is not part of the `PrimaryActionEvent` union, causing TypeScript compile failure.

## Fix (final)
```bash
# 1) Update the invalid primary action literal in CategoryRegistrationStats:
#    level_publish -> event: 'schedule-times'
# 2) Run build verification:
npm run build
npm run build:log
```

## Verification
- [x] Fingerprint captured and documented (`20d5d4c3`)
- [x] Artifact path recorded
- [x] Full build passes: `npm run build:log` exits 0

## Related Issues
- `docs/debug-kb/94b5e479.md`
- `docs/debug-kb/2f6d5e1b.md`

## Notes
- Fixed on 2026-03-14. Historical attempts retained for audit context.
