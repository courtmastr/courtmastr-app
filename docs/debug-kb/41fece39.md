---
id: 41fece39
title: "lint:log fails on existing repository-wide ESLint baseline violations"
signature: "eslint repo scan reports existing non-task violations across e2e scripts src and tests"
area: lint
status: 🟡 workaround
tags: [lint, baseline, eslint]
artifact: 2026-03-12-21-27-31.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails on existing repository-wide ESLint baseline violations

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Front Desk and scorer UI refresh in `src/features/checkin/` and `src/features/scoring/`.

## Error Signature
```
Repository-wide ESLint run fails with existing violations in unrelated e2e, scripts,
Vue component, and test files outside the registration task scope.
```

## Attempts

### Attempt 1
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after the public registration change.

**Result**: 🟡 Failed with unrelated baseline errors, including `agent-browser-fixtures.ts`, multiple `scripts/*.cjs|mjs` files, `GenerateBracketDialog.vue`, `CompactDataTable.vue`, and existing test-file warnings. No lint errors were reported in `RegisterView.vue`, `RegisterView.test.ts`, or `user-registration.spec.ts`.

### Attempt 2
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after adding volunteer access core helpers, callable wrappers, and the new focused unit test.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures. No lint errors were reported in `functions/src/volunteerAccessCore.ts`, `functions/src/volunteerAccess.ts`, or `tests/unit/volunteerAccessCore.test.ts`.

### Attempt 3
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after adding the volunteer session store, volunteer access view, layout split, and router guard logic, then fixing the temporary empty-interface lint issue in `src/stores/volunteerAccess.ts`.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors remained in the new volunteer access files.

### Attempt 4
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after adding volunteer-aware mutation routing in the registration and match stores plus the callable-backed functions changes.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were reported in `src/stores/registrations.ts`, `src/stores/matches.ts`, `functions/src/updateMatch.ts`, or `functions/src/volunteerAccess.ts`.

### Attempt 5
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after fixing numeric primary-id queries in `functions/src/storage/firestore-adapter.ts` and adding `tests/unit/firestoreAdapter.test.ts`.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were reported in `functions/src/storage/firestore-adapter.ts` or `tests/unit/firestoreAdapter.test.ts`.

### Attempt 6
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after volunteer scorekeeper kiosk routing/mobile scoring updates and the bracket foreign-key adapter fix.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were reported in `src/features/scoring/views/MatchListView.vue`, `src/features/scoring/views/ScoringInterfaceView.vue`, `src/router/index.ts`, `functions/src/storage/firestore-adapter.ts`, or the new volunteer scoring tests.

### Attempt 7
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after resolving stash-pop merge conflicts between sponsor-branding and local volunteer/public-player worktree changes.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were reported in the resolved conflict files (`TournamentSettingsView.vue`, `PublicPlayerView.vue`, `src/types/index.ts`).

### Attempt 8
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after Playwright suite cleanup (feature-based projects/reporting and setup log-noise reduction in `e2e/auth.setup.ts`).

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were introduced in the Playwright config/scripts changes.

### Attempt 9
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after Front Desk and scorer experience redesign updates.

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were reported in `src/features/checkin/views/FrontDeskCheckInView.vue`, `src/features/checkin/components/RapidCheckInPanel.vue`, `src/features/checkin/components/BulkCheckInPanel.vue`, `src/features/scoring/views/MatchListView.vue`, or `src/features/scoring/views/ScoringInterfaceView.vue`.

### Attempt 10
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after Playwright suite cleanup and scoring-feature test stabilization (`e2e/auth.setup.ts`, `e2e/scorekeeper-flow.spec.ts`, `e2e/concurrent-five-scorers.spec.ts`).

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were introduced in the Playwright files touched by this task.

### Attempt 11
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after final Playwright test stabilization pass (`CP-066` pattern addition and scoring-concurrency assertion hardening).

**Result**: 🟡 Same baseline fingerprint (`41fece39`) with unrelated repo-wide lint failures only. No lint errors were introduced in the Playwright files touched by this task.

## Root Cause
The repository has existing ESLint violations outside the files touched by the public registration task, so the required full lint run does not currently provide a green signal for scoped changes.

## Fix (final)
```bash
# Workaround for scoped tasks:
# 1) Record the baseline lint fingerprint and artifact.
# 2) Verify task files with targeted tests and build evidence where possible.
npm run test:log -- --run tests/unit/RegisterView.test.ts
```

## Verification
- [x] Fingerprint captured and documented (`41fece39`)
- [x] Artifact path recorded
- [ ] Full lint passes: `npm run lint:log` exits 0 (blocked by baseline)

## Related Issues
- `docs/debug-kb/9ce24fd4.md`

## Notes
- This fingerprint is a newer baseline snapshot of the same repo-wide lint condition.
