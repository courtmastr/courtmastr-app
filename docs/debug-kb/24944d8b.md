---
id: 24944d8b
title: "vue-tsc build fails with TypeScript errors"
signature: "vue-tsc -b failed with TypeScript errors"
area: build
status: ❌ unresolved
tags: [build, typescript, vue-tsc]
artifact: 2026-02-04-17-06-53.vue-tsc-b.log
---

# KB Entry: vue-tsc build fails with TypeScript errors

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Updated scheduling paths, dashboard match subscription, auto-schedule dialog/test selectors

## Error Signature
```
error TS6133: '...' is declared but its value is never read.
```

## Attempts

### Attempt 1
**Date**: 2026-02-04
**Change**: Ran `npm run build:log` after code changes.

**Result**:
❌ Build failed with multiple pre-existing TypeScript errors across unrelated files.

## Root Cause
Unresolved; current build output shows multiple pre-existing TypeScript errors in unrelated components (unused imports, missing types).

## Fix (final)
Not resolved. Next step to try:
```bash
# Address reported TypeScript errors, then rerun build
npm run build:log
```

## Verification
- [ ] Build passes: `npm run build:log` exits 0

## Related Issues
- None

## Notes
- Errors include unused imports and missing type properties in multiple tournament components.
