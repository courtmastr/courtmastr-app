---
id: 77e0bc7c
title: "lint:log with file args still lints whole workspace and fails"
signature: "eslint . --ext ... --fix <file args> still reports unrelated workspace errors"
area: lint
status: ❌ unresolved
tags: [lint, tooling, workspace]
artifact: 2026-02-16-02-15-38.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log with file args still lints whole workspace and fails

## Context
- Command: `npm run lint:log -- src/components/common/FilterBar.vue ...`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: TOURNEY-105 filter bar standardization

## Error Signature
```text
run-and-log executes:
eslint . --ext ... --fix --ignore-path .gitignore <file args>

Result: still lints full repo (.) and fails on unrelated files
```

## Attempts

### Attempt 1
**Date**: 2026-02-16  
**Change**: Passed explicit file paths to `npm run lint:log` expecting scoped lint.  
**Result**: ❌ Command still starts with `eslint .` and reports unrelated failures.

## Root Cause
`lint:log` script always invokes `eslint .` so additional file arguments do not narrow scope enough to avoid unrelated workspace failures.

## Fix (final)
Pending. Use direct scoped `npx eslint <files>` for ticket validation or update `lint:log` script to accept a scoped target.

## Verification
- [ ] `npm run lint:log` supports scoped targets
- [x] `npx eslint` passes on ticket-touched files

## Related Issues
- `docs/debug-kb/9d03826c.md`

## Notes
- This is a tooling limitation, not a TOURNEY-105 regression.
