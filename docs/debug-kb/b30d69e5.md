---
id: b30d69e5
title: "lint:log fails because @typescript-eslint/eslint-plugin is missing"
signature: "ESLint couldn't find the plugin '@typescript-eslint/eslint-plugin'"
area: lint
status: ✅ fixed
tags: [lint, eslint, dependency]
artifact: 2026-02-23-00-48-03.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails because @typescript-eslint/eslint-plugin is missing

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Re-schedule workflow updates, assignment gate test coverage, and lint script compatibility fix.

## Error Signature
```text
ESLint couldn't find the plugin "@typescript-eslint/eslint-plugin"
The plugin "@typescript-eslint/eslint-plugin" was referenced from ".eslintrc.cjs"
```

## Attempts

### Attempt 1
**Date**: 2026-02-23  
**Change**: Re-ran `npm run lint:log` after switching lint scripts to legacy `.eslintrc` mode (`ESLINT_USE_FLAT_CONFIG=false`) to resolve unsupported `--ignore-path`.

**Result**:
❌ New fingerprint `b30d69e5`; command now advances past CLI flag parsing and fails on missing lint plugin dependency.

### Attempt 2
**Date**: 2026-02-23  
**Change**: Re-ran `npm run lint:log` after the re-schedule partition budget fix to verify the full gate status for this change set.

**Result**:
❌ Same fingerprint `b30d69e5`; lint still fails before project analysis because `@typescript-eslint/eslint-plugin` is not installed.

### Attempt 3
**Date**: 2026-02-23  
**Change**: Re-ran `npm run lint:log` after consolidating Public Schedule + Display Mode routes/views to verify lint status for this feature update.

**Result**:
❌ Same fingerprint `b30d69e5`; lint remains blocked by missing `@typescript-eslint/eslint-plugin`.
No dependency changes were applied in this task because agent rules prohibit adding new packages without explicit user instruction.

### Attempt 4
**Date**: 2026-02-23  
**Change**: Re-ran `npm run lint:log` after final build/test verification to confirm whether lint gate was affected by subsequent fixes.

**Result**:
❌ Same fingerprint `b30d69e5`; still blocked on missing `@typescript-eslint/eslint-plugin`.

### Attempt 5
**Date**: 2026-02-23  
**Change**: Installed missing plugin with `npm install -D @typescript-eslint/eslint-plugin`, then re-ran `npm run lint:log`.

**Result**:
✅ Missing-plugin failure resolved. Lint now proceeds and reports project lint violations under a new fingerprint (`8546e9a4`).

## Root Cause
Project ESLint config referenced `@typescript-eslint/eslint-plugin`, but that package was not installed.

## Fix (final)
```bash
npm install -D @typescript-eslint/eslint-plugin
npm run lint:log
```

## Verification
- [x] Missing-plugin fingerprint `b30d69e5` no longer occurs
- [x] Fingerprint `159d0de4` (unsupported `--ignore-path`) no longer occurs

## Related Issues
- `/Users/ramc/Documents/Code/courtmaster-v2/docs/debug-kb/159d0de4.md`
- `/Users/ramc/Documents/Code/courtmaster-v2/docs/debug-kb/0615d796.md`

## Notes
- Current lint blocker moved to fingerprint `8546e9a4` (actual lint-rule violations).
