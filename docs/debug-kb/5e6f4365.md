---
id: 5e6f4365
title: "Build fails when test helper narrows brackets-model participant IDs to numbers"
signature: "error TS2322: Type 'Id | null | undefined' is not assignable to type 'number | null | undefined'"
area: build
status: ✅ fixed
tags: [typescript, tests, brackets]
artifact: 2026-04-16-20-30-53.npm-run-check-firebase-env-vue-tsc-b-vite-build.log
---

# KB Entry: Build fails when test helper narrows brackets-model participant IDs to numbers

## Context
- Command: `npm run build`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added single-elimination bracket viewer normalization and regression tests for 9-player and 10-player brackets.

## Error Signature
```
error TS2322: Type 'Id | null | undefined' is not assignable to type 'number | null | undefined'
```

## Attempts

### Attempt 1
**Date**: 2026-04-16
**Change**: Checked the failing lines in `tests/unit/bracketViewerNormalization.test.ts` and widened the helper tuple type to accept the library's `Id` shape (`string | number | null | undefined`).

**Result**: ✅ `npm run build` and `npm run build:log` both passed after the type fix.

## Root Cause
The regression test helper incorrectly narrowed `brackets-model` participant IDs to `number`, but the library types allow `string | number`.

## Fix (final)
```bash
# Update the helper tuple type to match brackets-model's Id union
# Then rerun the build gates
npm run build
npm run build:log
```

## Verification
- [x] Build passes: `npm run build`
- [x] Logged build passes: `npm run build:log`

## Related Issues
- None

## Notes
- `brackets-model` uses the broader `Id` union for participant IDs, so test helpers should not narrow to `number` unless they explicitly coerce first.
