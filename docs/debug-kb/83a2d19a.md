# 83a2d19a

- Status: ✅ fixed
- Date: 2026-04-16
- Command: `npm run test:log -- --run tests/unit/ScoringQrDialog.test.ts`

## Root Cause

The test tried to inspect a `v-text-field` rendered inside a stubbed `BaseDialog`, so shallow mounting removed the child tree and the assertion was targeting markup that did not exist in the test harness.

## Fix (final)

1. Keep `shallowMount` so the dialog shell stays isolated.
2. Assert the route resolution, QR payload, and clipboard copy value instead of reading a stubbed child component tree.
3. Leave the rendered-field inspection to higher-level UI tests if needed.

## Verification

```bash
npm run test:log -- --run tests/unit/ScoringQrDialog.test.ts
```
