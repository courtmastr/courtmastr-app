---
id: e6b663c4
title: "test:log fails on women category alias colliding with men substring"
signature: "vitest assertion failed: expected alias WS but received MS for Women's Singles"
area: test
status: ✅ fixed
tags: [scheduling, alias, identity-codes]
artifact: 2026-02-27-18-23-48.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log
---

# KB Entry: test:log fails on women category alias colliding with men substring

## Context
- Command: `npm run test:log -- run tests/unit/matchDisplayIdentity.test.ts tests/unit/scheduleExport.test.ts`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added alias regex helper in `matchDisplayIdentity.ts` and export identity columns.

## Error Signature
```
AssertionError: expected 'MS' to be 'WS'
```

## Attempts

### Attempt 1
**Date**: 2026-02-27  
**Change**: Replaced naive substring checks with helper + apostrophe includes fallback.

**Result**: ❌ Still fails because `"women's"` contains `"men's"` substring when using plain `includes`.

### Attempt 2
**Date**: 2026-02-27  
**Change**: Replaced substring-based gender checks with strict word-level regex:
- `isWomen = /\\bwomen(?:'s)?\\b/i`
- `isMen = /\\bmen(?:'s)?\\b/i`

**Result**: ✅ Alias tests pass (`WS`/`WD` no longer collide with `MS`/`MD`).

## Root Cause
Alias parsing used substring fallback checks, so `"women's"` incorrectly matched `"men's"`.

## Fix (final)
Use strict word-level regex for category gender detection and avoid direct substring fallback.

## Verification
- [x] Failure reproduced with `test:log`
- [x] Fingerprint captured: `e6b663c4`
- [x] Artifact logged at `docs/debug-kb/_artifacts/2026-02-27-18-23-48.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log`
- [x] Fix verified: `npm run test:log -- run tests/unit/matchDisplayIdentity.test.ts tests/unit/scheduleExport.test.ts` (pass artifact: `2026-02-27-18-24-43.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log`)

## Related Issues
- `e429a6b6`

## Notes
- Use strict word-level regex for `men` and `women`; do not use direct substring fallback.
