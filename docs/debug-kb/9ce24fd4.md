---
id: 9ce24fd4
title: "lint:log fails on existing repository-wide ESLint baseline violations"
signature: "eslint repo scan reports existing non-task violations across e2e/scripts/src/tests"
area: lint
status: 🟡 workaround
tags: [lint, baseline, eslint]
artifact: 2026-03-03-17-32-21.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails on existing repository-wide ESLint baseline violations

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: MCIA scheduler updates and pool-stage layout work in separate branch/worktree.

## Error Signature
```
Repository-wide ESLint run fails with existing violations in unrelated files
(e2e fixtures, scripts, Vue components, and test files).
```

## Attempts

### Attempt 1
**Date**: 2026-03-02
**Change**: Ran full `npm run lint:log` after seed-script changes.

**Result**: ❌ Failed with same baseline repository violations; no errors from newly added seed files.

### Attempt 2
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after adding MCIA pool-result updater script.

**Result**: ❌ Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations.

### Attempt 3
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after validating and applying MCIA 2026 results updater.

**Result**: ❌ Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations.

### Attempt 4
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after leaderboard post-clinch-game fix and wording updates.

**Result**: ❌ Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations.

### Attempt 5
**Date**: 2026-03-03
**Change**: Ran full lint command after implementing scoped pool-stage layout changes.

**Result**: 🟡 Same baseline repository errors persist; no lint errors introduced in edited layout files.

### Attempt 6
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after adding UI audit remediation design and implementation planning docs.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations in existing e2e/scripts/src/tests files.

### Attempt 7
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after Public Schedule alert-state fix and test additions.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations; no lint errors were introduced in modified public schedule files.

### Attempt 8
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after queue urgency threshold updates and disabled-action clarity fixes.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repo-wide violations; task files did not add new lint failures.

### Attempt 9
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after read-only queue/live-view alignment and overlay URL responsiveness updates.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unchanged repository-wide violations outside task scope.

### Attempt 10
**Date**: 2026-03-03
**Change**: Re-ran `npm run lint:log` after adding urgent-item disabled-reason coverage in check-in workflow tests.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unchanged repository-wide errors and warning profile.

### Attempt 11
**Date**: 2026-03-11
**Change**: Re-ran `npm run lint:log` after adding tournament branding types and the `useTournamentBranding` normalization composable in the sponsor-branding worktree.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repository-wide lint violations; no branding-specific lint failures surfaced.

### Attempt 12
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after adding tournament branding storage helpers and Storage rules for tournament assets.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repository-wide lint violations; no storage-layer lint failures surfaced.

### Attempt 13
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after adding the tournament branding settings card and save flow for tournament and sponsor logo management in the sponsor-branding worktree.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repository-wide lint violations; no branding settings lint failures surfaced.

### Attempt 14
**Date**: 2026-03-12
**Change**: Re-ran `npm run lint:log` after integrating tournament and sponsor branding into shared components, tournament views, public views, and the overlay layout.

**Result**: 🟡 Same baseline fingerprint (`9ce24fd4`) with unrelated repository-wide lint violations; no branding integration lint failures surfaced.

## Root Cause
The repository has pre-existing ESLint violations outside the files modified in current tasks, causing full lint to fail.

## Fix (final)
```bash
# Workaround for scoped tasks:
# 1) Record baseline lint fingerprint in Debug KB.
# 2) Validate task scope with targeted tests/build evidence.
npm run test:log -- --run <targeted-tests>
```

## Verification
- [x] Fingerprint captured and documented (`9ce24fd4`)
- [x] Artifact path recorded
- [ ] Full lint passes: `npm run lint:log` exits 0 (blocked by baseline)

## Related Issues
- `docs/debug-kb/695ac768.md`
- `docs/debug-kb/a4894141.md`

## Notes
- This is a known baseline/workspace issue and not introduced by scoped feature changes.
