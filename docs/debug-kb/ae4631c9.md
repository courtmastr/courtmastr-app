---
id: ae4631c9
title: "lint:log fails when a hoisted Vitest mock uses bare require('vue')"
signature: "error  A `require()` style import is forbidden  @typescript-eslint/no-require-imports"
area: lint
status: ✅ fixed
tags: [lint, vitest, vue, hoisted-mocks]
artifact: 2026-04-19-06-13-11.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails when a hoisted Vitest mock uses bare require('vue')

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2/.worktrees/pool-standings-single-source`
- Recent changes: Added public tournament route tests for bracket-tab navigation and used a hoisted Vitest mock that needed Vue runtime helpers.

## Error Signature
```
error  A `require()` style import is forbidden  @typescript-eslint/no-require-imports
```

## Attempts

### Attempt 1
**Date**: 2026-04-19
**Change**: Left the hoisted test runtime using a direct `require('vue')` inside the `vi.hoisted()` block.

**Result**: ❌ `npm run lint:log` failed in `tests/unit/TournamentPublicPageView.test.ts`.

### Attempt 2
**Date**: 2026-04-19
**Change**: Wrapped the `require('vue')` call in a tiny IIFE and attached a targeted `eslint-disable-next-line @typescript-eslint/no-require-imports` comment directly to the offending line, while preserving the typed runtime helper:

```ts
...(() => {
  /* eslint-disable-next-line @typescript-eslint/no-require-imports */
  const vue = require('vue') as typeof import('vue');
  return { ...vue };
})(),
```

**Result**: ✅ `npm run lint:log` completed successfully.

## Root Cause
The hoisted Vitest mock needed runtime access to Vue before normal ESM imports were available, but the raw `require('vue')` line violated the repo ESLint rule until the suppression was placed on the exact offending statement.

## Fix (final)
```bash
# Update the hoisted test runtime to isolate the require call and suppress only that line
# then rerun lint
npm run lint:log
```

## Verification
- [x] Lint passes: `npm run lint:log`
- [x] The test file still builds with the typed Vue runtime helper intact

## Related Issues
- `20c982e5` — similar lint failure caused by hoisted Vitest runtime helpers using `require('vue')`

## Notes
- Hoisted Vitest factories can require CommonJS access for runtime helpers; keep the suppression as narrow as possible and preserve explicit typing on the required module.
