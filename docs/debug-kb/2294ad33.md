---
id: 2294ad33
title: "build:log fails on existing repository-wide TypeScript baseline errors"
signature: "vue-tsc -b reports unrelated TS errors across existing src/tests"
area: build
status: 🟡 workaround
tags: [vue-tsc, baseline, typescript]
artifact: 2026-03-03-14-17-02.vue-tsc-b.log
---

# KB Entry: build:log fails on existing repository-wide TypeScript baseline errors

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2/.worktrees/feat-pool-stage-layout`
- Recent changes: Pool-stage layout UI update in `BracketsManagerViewer` and new `stageLayout` helper/test.

## Error Signature
```
vue-tsc -b reports many existing TS errors in unrelated files (src/tests), exiting with code 2.
```

## Attempts

### Attempt 1
**Date**: 2026-03-03
**Change**: Re-ran build after fixing new `stageLayout` helper typing issue to isolate local changes.

**Result**: 🟡 Same repository-wide baseline TS failures remain (unrelated to this change scope).

## Root Cause
The branch contains pre-existing repository-wide TypeScript debt that blocks `vue-tsc -b` independent of the pool-layout change.

## Fix (final)
No scoped fix applied in this task; treat as baseline and validate change set with targeted tests plus lint result.

## Verification
- [x] New targeted tests pass: `npm run test:log -- --run tests/unit/stageLayout.test.ts`
- [ ] Full build passes (blocked by baseline)

## Related Issues
- Existing `build:log` baseline-error entries in `docs/debug-kb/index.yml`.

## Notes
- Keep this as workaround until repository-wide TS baseline cleanup is scheduled.
