---
id: 2f6d5e1b
title: "build:log fails on workspace TypeScript baseline errors (updated fingerprint)"
signature: "vue-tsc -b reports pre-existing type errors across unrelated source and test files"
area: build
status: 🟡 workaround
tags: [build, typescript, baseline]
artifact: 2026-03-03-00-53-00.vue-tsc-b.log
---

# KB Entry: build:log fails on workspace TypeScript baseline errors (updated fingerprint)

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Leaderboard post-clinch-game aggregation fix, MCIA results data correction, and related unit test.

## Error Signature
```
vue-tsc -b fails with repository-wide TypeScript errors in unrelated Vue components,
composables, and tests.
```

## Attempts

### Attempt 1
**Date**: 2026-03-03
**Change**: Ran full `npm run build:log` after leaderboard and MCIA results fixes.

**Result**: ❌ Failed with baseline workspace TypeScript errors; fingerprint surfaced as `2f6d5e1b` with artifact `2026-03-03-00-40-33...`.

### Attempt 2
**Date**: 2026-03-03
**Change**: Re-ran `npm run build:log` after leaderboard wording update (`Games` -> `Set W-L`).

**Result**: ❌ Same baseline fingerprint (`2f6d5e1b`) with unrelated workspace TypeScript errors; updated artifact `2026-03-03-00-53-00...`.

## Root Cause
The workspace contains pre-existing TypeScript errors outside the modified leaderboard and MCIA scripts; signature changed due touched-file context while underlying baseline remains.

## Fix (final)
```bash
# Workaround used for this task:
# 1) Record the updated build fingerprint in Debug KB.
# 2) Verify changed behavior with focused tests and runtime checks.
npm run test:log -- tests/unit/leaderboard.test.ts --run
npm run results:mcia2026:local
```

## Verification
- [x] Fingerprint captured and documented (`2f6d5e1b`)
- [x] Artifact path recorded
- [x] Changed-scope tests pass (`tests/unit/leaderboard.test.ts`)
- [ ] Full build passes: `npm run build:log` exits 0 (blocked by baseline)

## Related Issues
- `docs/debug-kb/94b5e479.md`
- `docs/debug-kb/740820fc.md`

## Notes
- This is a workspace baseline issue and not introduced by the current leaderboard or MCIA data fix.
