---
id: 5a37fca9
title: "RegisterView public signup test fails because privileged roles are still exposed"
signature: "AssertionError expected role options to not include Tournament Organizer"
area: test
status: ✅ fixed
tags: [auth, registration, roles]
artifact: 2026-03-11-20-58-03.vitest-run-tests-unit-RegisterView-test-ts.log
---

# KB Entry: RegisterView public signup test fails because privileged roles are still exposed

## Context
- Command: `npm run test:log -- --run tests/unit/RegisterView.test.ts`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added a focused unit test for Task 1 of the volunteer PIN access plan to verify public signup only exposes player-safe registration.

## Error Signature
```
AssertionError: expected [ 'Player', …(2) ] to not include 'Tournament Organizer'
```

## Attempts

### Attempt 1
**Date**: 2026-03-11
**Change**: Added a unit test that inspects `RegisterView.roleOptions` and asserts organizer and scorekeeper are not offered in public signup.

**Result**: ❌ Failed as expected because `RegisterView.vue` still includes `Tournament Organizer` and `Scorekeeper` in `roleOptions`.

### Attempt 2
**Date**: 2026-03-11
**Change**: Fixed `RegisterView.vue` to keep `selectedRole` locked to `player`, removed the public role selector, added player-safe explanatory copy, and updated the unit/E2E registration assertions.

**Result**: ✅ `tests/unit/RegisterView.test.ts` passed and `e2e/user-registration.spec.ts` passed on `chromium-no-auth`.

## Root Cause
Public registration still exposes privileged `organizer` and `scorekeeper` role options in `RegisterView.vue`.

## Fix (final)
```bash
# 1) Lock RegisterView public signup to player-only
# 2) Remove the role selector from the register page
# 3) Add explanatory copy that staff/volunteer access comes from tournament settings
# 4) Update the focused unit and E2E registration assertions
npm run test:log -- --run tests/unit/RegisterView.test.ts
npx playwright test e2e/user-registration.spec.ts --project=chromium-no-auth
```

## Verification
- [x] Failure reproduced with targeted test command
- [x] Unit test passes: `npm run test:log -- --run tests/unit/RegisterView.test.ts`
- [x] Registration UI no longer exposes privileged roles
- [x] Browser flow passes: `npx playwright test e2e/user-registration.spec.ts --project=chromium-no-auth`

## Related Issues
- None

## Notes
- Full `npm run lint:log` remains blocked by unrelated repo-wide ESLint baseline issues (`41fece39`).
- Full `npm run build:log` remains blocked by the existing `CategoryRegistrationStats.vue` type baseline (`20d5d4c3`).
