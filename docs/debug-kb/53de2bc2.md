---
id: 53de2bc2
title: "build:log fails with baseline TS errors plus temporary StageLike type mismatch"
signature: "vue-tsc -b fails; includes baseline TS errors and StageLike excess-property test mismatch"
area: build
status: 🟡 workaround
tags: [vue-tsc, baseline, tdd]
artifact: 2026-03-03-14-16-22.vue-tsc-b.log
---

# KB Entry: build:log fails with baseline TS errors plus temporary StageLike type mismatch

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2/.worktrees/feat-pool-stage-layout`
- Recent changes: Added `stageLayout` helper + test and pool-stage CSS updates.

## Error Signature
```
vue-tsc -b reports repository-wide TS baseline errors; run also included TS2353 on tests/unit/stageLayout.test.ts for missing StageLike.id
```

## Attempts

### Attempt 1
**Date**: 2026-03-03
**Change**: Added optional `id` to `StageLike` in `src/features/brackets/utils/stageLayout.ts`.

**Result**: 🟡 Local TS2353 issue removed; `build:log` still fails due unrelated repository baseline errors (new fingerprint `2294ad33`).

## Root Cause
The command was blocked by pre-existing repository TS baseline issues; one temporary local typing mismatch (`StageLike.id`) contributed to this specific fingerprint.

## Fix (final)
Scoped fix applied for local typing mismatch; baseline TS debt remains out of scope for this task.

## Verification
- [x] `npm run test:log -- --run tests/unit/stageLayout.test.ts` passes after helper type fix
- [ ] `npm run build:log` full pass (blocked by baseline)

## Related Issues
- `2294ad33` (build baseline workaround)

## Notes
- Keep as workaround record for this fingerprint transition.
