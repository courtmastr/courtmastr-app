---
id: e2c5e36f
title: "direct vite build command not found in run-and-log shell"
signature: "/bin/sh: vite: command not found"
area: build
status: ❌ unresolved
tags: [build, vite, tooling]
artifact: 2026-02-18-06-33-11.vite-build.log
---

# KB Entry: direct vite build command not found in run-and-log shell

## Context
- Command: `node scripts/run-and-log.mjs vite build`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: trying fallback hosting build path after `build:log` baseline TypeScript failure.

## Error Signature
```text
/bin/sh: vite: command not found
```

## Attempts

### Attempt 1
**Date**: 2026-02-18  
**Change**: Invoked `vite build` directly through run-and-log.
**Result**: ❌ Shell couldn’t resolve local Vite binary.

## Root Cause
Direct shell command execution did not resolve local `node_modules/.bin` for `vite`.

## Fix (final)
Use `npx vite build` (or an npm script) so the local Vite binary resolves.

## Verification
- [ ] Frontend build command exits 0
- [ ] `dist/` updated for deploy

## Related Issues
- `docs/debug-kb/f44de9d5.md`

## Notes
- This is command invocation/tooling scope, not app code failure.
