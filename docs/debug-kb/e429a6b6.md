---
id: e429a6b6
title: "test:log fails on category alias parsing and missing schedule export display identity fields"
signature: "vitest assertion failed: Women's alias resolved as MS; schedule export rows missing displayCode/globalMatchKey"
area: test
status: ✅ fixed
tags: [scheduling, identity-codes, export]
artifact: 2026-02-27-18-20-39.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log
---

# KB Entry: test:log fails on category alias parsing and missing schedule export display identity fields

## Context
- Command: `npm run test:log -- run tests/unit/matchDisplayIdentity.test.ts tests/unit/scheduleExport.test.ts`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added tests for per-category display codes (`MS1`, `MD1`, `MXD1`) and export columns (`displayCode`, `globalMatchKey`).

## Error Signature
```
AssertionError: expected 'MS' to be 'WS'
AssertionError: expected undefined to be 'MS1'
```

## Attempts

### Attempt 1
**Date**: 2026-02-27  
**Change**: Added failing tests to validate alias parsing and export identity fields before implementation.

**Result**: ❌ Fails as expected; implementation updates required in `matchDisplayIdentity.ts` and `scheduleExport.ts`.

### Attempt 2
**Date**: 2026-02-27  
**Change**: Added export identity support in `scheduleExport.ts`:
- build display codes from category aliases
- include `displayCode` and `globalMatchKey` in `ScheduleExportRow`
- add both columns to generated Excel output

**Result**: ✅ Export identity tests now pass; remaining alias collision moved to fingerprint `e6b663c4`.

## Root Cause
Export utility did not include new identity fields needed for compact relinking (`displayCode`, `globalMatchKey`).

## Fix (final)
Updated `buildScheduleExportRows` to compute identity values from `buildDisplayCodeMap` and `buildGlobalMatchKey`, and included both in Excel columns.

## Verification
- [x] Failure reproduced with `test:log`
- [x] Fingerprint captured: `e429a6b6`
- [x] Artifact logged at `docs/debug-kb/_artifacts/2026-02-27-18-20-39.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log`
- [x] Fix verified: `npm run test:log -- run tests/unit/matchDisplayIdentity.test.ts tests/unit/scheduleExport.test.ts` (pass artifact: `2026-02-27-18-24-43.vitest-run-tests-unit-matchDisplayIdentity-test-ts.log`)

## Related Issues
- None

## Notes
- `"women"` contains substring `"men"`; alias detection must check women/mixed before men or use word boundaries.
