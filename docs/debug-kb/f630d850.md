---
id: f630d850
title: "ScoringInterface unit test misses Complete Game text under shallow stubs"
signature: "wrapper.text() is empty under shallowMount stubs even though computed lock state is true"
area: test
status: ✅ fixed
tags: [vitest, vue-test-utils, scoring]
artifact: 2026-03-12-17-27-19.vitest-run-tests-unit-matches-scoring-store-test-t.log
---

# KB Entry: ScoringInterface unit test misses Complete Game text under shallow stubs

## Context
- Command: `npm run test:log -- --run tests/unit/matches.scoring.store.test.ts tests/unit/ScoringInterfaceView.volunteerMode.test.ts`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added pending-completion lock tests for scorer behavior.

## Error Signature
```
AssertionError: expected '' to contain 'Complete Game'
```

## Attempts

### Attempt 1
**Date**: 2026-03-12
**Change**: Added text assertion for `Complete Game` in a shallow-mounted component tree.

**Result**: ❌ Stubbed Vuetify components suppressed rendered text in the test wrapper output.

## Root Cause
`shallowMount` with stubbed Vuetify components did not reliably include nested button text in `wrapper.text()`, producing a false-negative assertion even when the UI logic was correct.

## Fix (final)
Replaced brittle raw-text assertions with state-and-behavior assertions:
1. Asserted `scoreEntryLocked` / `currentGameReadyToComplete` computed state.
2. Asserted scorer behavior (`addPoint` blocked, `removePoint` allowed).
3. Asserted explicit `completeCurrentGame` store action invocation.

## Verification
- [x] `npm run test:log -- --run tests/unit/matches.scoring.store.test.ts tests/unit/ScoringInterfaceView.volunteerMode.test.ts`

## Related Issues
- [a79a28bf](./a79a28bf.md)
