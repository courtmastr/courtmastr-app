---
id: 2627fce6
title: "lint:log fails on existing repository-wide ESLint baseline violations during workflow fix verification"
signature: "eslint exits with existing repository-wide errors outside touched workflow files"
area: lint
status: 🟡 workaround
tags: [lint, baseline, verification]
artifact: 2026-03-19-19-13-01.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails on existing repository-wide ESLint baseline violations during workflow fix verification

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Fixed category publish CTA wiring, fixed Match Control auto-assign reactivity, added regression tests, updated coding pattern guide.

## Error Signature
```text
eslint exits with existing repository-wide errors in unrelated files (scripts/, legacy components, and older tests) while touched workflow files remain valid.
```

## Attempts

### Attempt 1
**Date**: 2026-03-19
**Change**: Ran `npm run lint:log` after the workflow fix to satisfy the logged lint gate.

**Result**: 🟡 Same repository-wide baseline errors surfaced in unrelated files including `scripts/*.cjs`, `src/components/*`, and older unit test files. No new lint error pointed at the workflow files changed in this fix.

## Root Cause
Repository-wide ESLint baseline violations outside the touched workflow files still cause `lint:log` to fail even when the local fix is clean.

## Fix (final)
```bash
# Workaround for narrow workflow fixes until the lint baseline is cleaned up:
npm run test:log -- --run tests/unit/CategoryRegistrationStats.test.ts tests/unit/CategoryRegistrationStats.pool-phase.test.ts tests/unit/MatchControlView.auto-assign.test.ts
npm run build
npm run build:log
```

## Verification
- [x] Targeted logged tests pass
- [x] Build passes: `npm run build`
- [x] Logged build passes: `npm run build:log`
- [ ] Repository-wide lint baseline is cleaned up

## Related Issues
- `docs/debug-kb/73e2457c.md`
- `docs/debug-kb/43636cee.md`
- `docs/debug-kb/9ce24fd4.md`

## Notes
- Treat this fingerprint as baseline noise unless the lint output points at files touched by the current change.
