---
id: 621a4693
title: "build:log fails with existing workspace TypeScript baseline errors"
signature: "vue-tsc -b reports widespread TS6133/TS2339/TS2345 errors across unrelated files"
area: build
status: ❌ unresolved
tags: [typescript, build, baseline]
artifact: 2026-02-18-18-53-04.vue-tsc-b.log
---

# KB Entry: build:log fails with existing workspace TypeScript baseline errors

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Updated tournament leaderboard with category summaries/filter support and BWF-style points/status columns.

## Error Signature
```
vue-tsc -b exits with many pre-existing TS errors in unrelated feature/admin/checkin/scoring files
```

## Attempts

### Attempt 1
**Date**: 2026-02-18
**Change**: Re-ran `npm run build:log` after Create Levels clarity updates and verified feature-specific tests.

**Result**:
❌ Full build still fails due existing workspace-wide TypeScript baseline issues.

### Attempt 2
**Date**: 2026-02-18
**Change**: Re-ran `npm run build:log` after Firestore rules update for level generation paths.

**Result**:
❌ Full build still fails due existing workspace-wide TypeScript baseline issues.

### Attempt 3
**Date**: 2026-02-18
**Change**: Re-ran `npm run build:log` after replacing `scripts/seed-simple.ts` with bracket generation + pool completion logic.

**Result**:
❌ Full build still fails due existing workspace-wide TypeScript baseline issues.

### Attempt 4
**Date**: 2026-02-18
**Change**: Re-ran `npm run build:log` after leaderboard category-filter/stats updates.

**Result**:
❌ Full build still fails due existing workspace-wide TypeScript baseline issues.

## Root Cause
Repository has unresolved TypeScript baseline errors across many unrelated files, so full `vue-tsc -b` cannot pass in this branch.

## Fix (final)
Not resolved in this ticket. Baseline TypeScript debt must be addressed independently before full build can pass.

## Verification
- [ ] Full build passes: `npm run build:log`
- [x] Feature-specific tests pass (`tests/unit/poolLeveling.test.ts`)

## Related Issues
- `docs/debug-kb/ad278b1c.md`
- `docs/debug-kb/2a901f46.md`

## Notes
- New fingerprint maps to the same broad baseline TS debt class as recent build failures.
