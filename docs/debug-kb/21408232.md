---
id: 21408232
title: "build:log fails with existing workspace TypeScript baseline errors"
signature: "vue-tsc -b reports widespread TS6133/TS2339/TS2345 errors across unrelated files"
area: build
status: ❌ unresolved
tags: [typescript, build, baseline]
artifact: 2026-02-18-08-01-38.vue-tsc-b.log
---

# KB Entry: build:log fails with existing workspace TypeScript baseline errors

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: pool-to-elimination starter implementation (bracket generation + dashboard wiring).

## Error Signature
```
vue-tsc -b exits with many pre-existing TS errors in admin/checkin/scoring/tournaments files unrelated to current change
```

## Attempts

### Attempt 1
**Date**: 2026-02-18
**Change**: Ran `npm run build:log` after implementing pool/elimination flow and then ran focused `vue-tsc` grep checks for modified files.

**Result**:
❌ Full build still fails due existing workspace-wide TypeScript baseline errors; modified files did not add new type errors.

## Root Cause
Repository currently has unresolved TypeScript baseline issues across many unrelated files, so `vue-tsc -b` cannot pass in this branch.

## Fix (final)
Not resolved in this ticket. Existing baseline must be cleaned first (or build scope narrowed) before full `build:log` can pass.

## Verification
- [ ] Full build passes: `npm run build:log` exits 0
- [x] Modified files are free of new targeted type errors (checked via filtered `vue-tsc` output)

## Related Issues
- `docs/debug-kb/f4dc4418.md`
- `docs/debug-kb/6e7ada5c.md`

## Notes
- This failure is unrelated to the pool/elimination implementation itself.
