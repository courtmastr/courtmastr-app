---
id: 159d0de4
title: "lint:log fails because eslint CLI rejects --ignore-path"
signature: "Invalid option '--ignore-path' - perhaps you meant '--ignore-pattern'?"
area: lint
status: ✅ fixed
tags: [lint, eslint, tooling]
artifact: 2026-02-23-00-20-49.eslint-ext-vue-js-jsx-cjs-mjs-ts-tsx-cts-mts-fix-i.log
---

# KB Entry: lint:log fails because eslint CLI rejects --ignore-path

## Context
- Command: `npm run lint:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Categories page card/menu redesign and category format/type label mapping fixes.

## Error Signature
```text
Invalid option '--ignore-path' - perhaps you meant '--ignore-pattern'?
You're using eslint.config.js, some command line flags are no longer available.
```

## Attempts

### Attempt 1
**Date**: 2026-02-22  
**Change**: Ran required `npm run lint:log` verification command after UI changes.  
**Result**: ❌ Command failed before linting project files due unsupported CLI flag in current ESLint runtime.

### Attempt 2
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after pool standings math fix (`W` column now uses actual wins).  
**Result**: ❌ Same fingerprint `159d0de4`; failure occurs before file linting because ESLint CLI rejects `--ignore-path`.

### Attempt 3
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after Schedule-phase/category-card/public-schedule implementation.  
**Result**: ❌ Same fingerprint `159d0de4`; command fails at startup with unsupported `--ignore-path` before linting source files.

### Attempt 4
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after Match Control All Matches schedule/public-state UI polish.
**Result**: ❌ Same fingerprint `159d0de4`; script still exits before linting due unsupported `--ignore-path`.

### Attempt 5
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after final time-sort null-placement tweak and verification pass.
**Result**: ❌ Same fingerprint `159d0de4`; no source linting runs because CLI argument parsing fails first.

### Attempt 6
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after public schedule UX and filtering updates.
**Result**: ❌ Same fingerprint `159d0de4`; command still exits before linting because ESLint rejects `--ignore-path`.

### Attempt 7
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after enforcing publish-before-assign guards and release workflow updates.  
**Result**: ❌ Same fingerprint `159d0de4`; lint command fails before scanning files due unsupported `--ignore-path` CLI argument.

### Attempt 8
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after assignment atomicity and planned-time fallback cleanup.  
**Result**: ❌ Same fingerprint `159d0de4`; unchanged tooling failure occurs at CLI argument parsing.

### Attempt 9
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after removing public schedule court-field rendering.  
**Result**: ❌ Same fingerprint `159d0de4`; command still fails pre-lint on unsupported `--ignore-path`.

### Attempt 10
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after unifying Categories scheduling to the `useMatchScheduler.scheduleMatches` path.
**Result**: ❌ Same fingerprint `159d0de4`; unchanged CLI tooling flag issue blocks lint execution.

### Attempt 11
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after implementing shared-court parallel mode in Auto Schedule dialog.
**Result**: ❌ Same fingerprint `159d0de4`; lint still fails before file checks due unsupported `--ignore-path`.

### Attempt 12
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after import-order cleanup and final shared-pool scheduling adjustments.
**Result**: ❌ Same fingerprint `159d0de4`; tooling mismatch persists before linting source files.

### Attempt 13
**Date**: 2026-02-22  
**Change**: Re-ran `npm run lint:log` after capping shared courts to available courts and blocking schedule when no courts are available.
**Result**: ❌ Same fingerprint `159d0de4`; command still fails during ESLint CLI argument parsing.

### Attempt 14
**Date**: 2026-02-23  
**Change**: Re-ran `npm run lint:log` during production-readiness audit after scheduling workflow alignment checks.
**Result**: ❌ Same fingerprint `159d0de4`; command still exits before linting due unsupported `--ignore-path`.

### Attempt 15
**Date**: 2026-02-23  
**Change**: Updated lint scripts to use legacy `.eslintrc` mode with `ESLINT_USE_FLAT_CONFIG=false`, then re-ran `npm run lint:log`.
**Result**: ✅ `159d0de4` resolved; lint now moves past CLI flag parsing and fails on a new dependency fingerprint (`b30d69e5`).

## Root Cause
ESLint v9 was running in flat-config mode, where `--ignore-path` is unsupported.

## Fix (final)
Set lint scripts to run with `ESLINT_USE_FLAT_CONFIG=false` so `.eslintrc.cjs` is honored and `--ignore-path` works.

## Verification
- [x] `npm run lint:log` no longer fails with unsupported `--ignore-path`
- [x] `npm run build:log` exits 0

## Related Issues
- `docs/debug-kb/41a15271.md`
- `docs/debug-kb/8c707ab7.md`

## Notes
- Superseded by `b30d69e5` (missing lint plugin dependency).
