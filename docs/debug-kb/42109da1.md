---
id: 42109da1
title: "build:log fails with existing workspace TypeScript baseline errors"
signature: "vue-tsc -b reports workspace-wide TS6133/TS2339/TS2307 errors"
area: build
status: ✅ fixed
tags: [build, typescript, baseline]
artifact: 2026-02-20-14-20-54.vue-tsc-b.log
---

# KB Entry: build:log fails with existing workspace TypeScript baseline errors

## Context
- Command: `npm run build:log`
- Working directory: `/Users/ramc/Documents/Code/courtmaster-v2`
- Recent changes: Added GitHub Actions CI/CD workflow for Firebase deploy on `master`.

## Error Signature
```text
vue-tsc -b fails with many existing TS errors across unrelated files
```

## Attempts

### Attempt 1
**Date**: 2026-02-20
**Change**: Ran required `build:log` validation after CI/CD workflow changes.

**Result**:
❌ Build failed due existing type-check baseline issues across admin/checkin/registration/scoring/tournaments and legacy diagnose test files.

### Attempt 2
**Date**: 2026-02-20
**Change**: Fixed shared model drift in `src/types/index.ts`, removed stale unused code across affected Vue files, scoped `tsconfig.app.json` includes to app source only, and reran `npm run build:log`.

**Result**:
✅ `npm run build:log` succeeded and produced a complete Vite production build.

## Root Cause
`vue-tsc -b` failed due a combination of stale unused declarations, shared type/interface drift, and build config including test files in app typecheck scope.

## Fix (final)
Applied targeted compile-fix cleanup and type alignment, then verified with logged build:

```bash
npm run build:log
```

## Verification
- [x] `npm run build:log` exits 0
- [x] `vue-tsc -b` completes with no TS errors
- [x] `vite build` executes after type check

## Related Issues
- `docs/debug-kb/9248ae25.md`
- `docs/debug-kb/6b0bf24f.md`
- `docs/debug-kb/6a900427.md`

## Notes
- This failure occurs before deployment and is a baseline quality issue, not introduced by this ticket.
